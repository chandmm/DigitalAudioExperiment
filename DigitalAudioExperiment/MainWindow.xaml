<Window x:Class="DigitalAudioExperiment.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DigitalAudioExperiment" 
        xmlns:view="clr-namespace:DigitalAudioExperiment.View"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Title="Digital Audio Experiment" 
        Height="{DynamicResource AppHeight}"
        Width="{DynamicResource AppWidth}"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Grid Background="Transparent">
        <view:ReceiverView DataContext="{StaticResource ReceiverViewModelKey}"/>
    </Grid>
</Window>
